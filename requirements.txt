# Dumbi-JARVIS Backend Requirements
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
pydantic>=2.0
python-dotenv>=1.0.0
requests>=2.31.0
aiohttp>=3.9.0
aiosqlite>=0.20.0
python-multipart>=0.0.9

# Web Search (Agentic, no API key required)
duckduckgo-search>=6.0.0

# Voice / Audio — STT
faster-whisper>=1.0.0
openai-whisper>=20231117

# Voice / Audio — Wake Word + VAD
openwakeword>=0.6.0
webrtcvad>=2.0.10
pyaudio>=0.2.14

# Voice / Audio — TTS (Piper neural + pyttsx3 fallback)
pyttsx3>=2.90
# piper-tts installed separately: pip install piper-tts
# piper model download: python -c "from app.core.tts_engine import download_piper_model; download_piper_model()"

# Evaluation & Debugging
# (sqlite3 is stdlib — no extra dep needed)

# Utilities
psutil>=5.9.0
numpy>=1.26.0

# Edge TTS (Microsoft neural voices; requires internet)
edge-tts>=6.1.0
